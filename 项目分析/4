基于项目分析和已完成的功能，我提出以下改进策略：

---

## 项目改进策略分析

### 一、项目现状总结

| 维度 | 完成度 | 说明 |
|------|--------|------|
| **核心物理模拟** | 90% | 5 个预设场景 + 自定义编辑器，欧拉积分 + CCD 碰撞检测 |
| **AI 生成功能** | 75% | 接入真实 API + 场景生成器，但 Prompt 准确率待优化 |
| **教学功能** | 80% | 受力可视化 + 回放 + 参数调节，缺题目库和题解 |
| **用户体验** | 70% | 编辑器体验好，但导出/撤销/移动端有短板 |
| **技术架构** | 85% | TS 迁移部分完成，组件拆分清晰 |

### 二、改进策略（按优先级分类）

#### 🔴 P0 级（核心竞争力，建议优先完成）

| 策略 | 收益 | 工作量 | 说明 |
|------|------|--------|------|
| **1. 预置高考真题库** | ⭐⭐⭐⭐⭐ | 2-3 天 | 20-50 道典型高考题 + 对应 JSON 场景，一键加载运行 |
| **2. AI Prompt 优化** | ⭐⭐⭐⭐ | 1-2 天 | 添加更多 few-shot 示例（弹簧振子/传送带/板块），温度调优，错误率降低 |
| **3. 部署在线体验** | ⭐⭐⭐⭐⭐ | 已完成 | GitHub Pages 已部署，需确保稳定访问 |

#### 🟡 P1 级（提升教学质量，中期完成）

| 策略 | 收益 | 工作量 | 说明 |
|------|------|--------|------|
| **4. 题目解析结果纠错 UI** | ⭐⭐⭐⭐ | 1 天 | 用户可手动修正 AI 解析错误的参数（当前有编辑功能，但不够直观） |
| **5. 关键帧标注功能** | ⭐⭐⭐⭐ | 1 天 | 回放时自动标注关键帧（速度变号、碰撞瞬间、最高点等） |
| **6. 多过程模拟支持** | ⭐⭐⭐ | 2-3 天 | AI 输出 stages 数组，按时间顺序切换场景约束 |
| **7. 单元测试覆盖** | ⭐⭐⭐ | 3-4 天 | Vitest 测试物理正确性（碰撞速度、落地时间等），保障可信度 |

#### 🟢 P2 级（体验优化，视时间决定）

| 筺略 | 收益 | 工作量 | 说明 |
|------|------|--------|------|
| **8. 移动端触屏适配** | ⭐⭐⭐ | 2-3 天 | 触屏手势支持，课堂互动演示 |
| **9. 图片识别（多模态）** | ⭐⭐⭐⭐⭐ | 3-4 天 | 接入 GPT-4V/GLM-4V，上传题目截图直接生成场景（决赛亮点） |
| **10. 导出/加载功能** | ⭐⭐ | 1 天 | JSON 文件保存/加载，替代剪贴板 |
| **11. 3D 模式** | ⭐⭐⭐⭐⭐ | 5-7 天 | Three.js/WebGL 3D 渲染，覆盖更多题型（报名时已提及） |
| **12. 国际化** | ⭐⭐ | 1-2 天 | 中英文切换，扩大用户群体 |

---

### 三、具体改进方案（重点推荐）

#### 1. 预置高考真题库（最高收益）

**实现方案**：
```typescript
// src/composables/useQuestionBank.ts
interface QuestionItem {
  id: string
  title: string        // "2024全国甲卷第18题"
  description: string  // 完整题目文本
  difficulty: 'easy' | 'medium' | 'hard'
  tags: string[]       // ['斜面', '碰撞', '磁场']
  sceneJson: ParsedProblem  // AI 返回的 JSON 结构
}

const questionBank: QuestionItem[] = [
  {
    id: 'q1',
    title: '斜面滑块基础题',
    description: '质量2kg滑块从倾角30°光滑斜面顶端由静止释放...',
    difficulty: 'easy',
    tags: ['斜面', '重力'],
    sceneJson: { topic: 'slope', objects: [...], ... }
  },
  // ... 20-50 道题目
]
```

**UI 设计**：
- 侧边栏题目列表，按难度/标签筛选
- 点击题目 → 自动加载场景并播放
- 题目描述显示在画布上方

#### 2. AI Prompt 优化策略

**当前问题**：
- 6 个 few-shot 示例，覆盖率约 70%
- 复杂题目（多物体/多过程）解析准确率低

**改进方案**：
```typescript
// 扩展 SYSTEM_PROMPT，添加以下示例：
示例7（多物体碰撞）：
题目："质量1kg和2kg的两球在光滑水平面上相向运动..."
输出：{ objects: [ball1, ball2], topic: 'elastic_collision' }

示例8（分阶段过程）：
题目："物体先在斜面加速，然后碰撞弹簧..."
输出：{ stages: [{phase: 'slope', ...}, {phase: 'spring', ...}] }

// 调整温度参数
temperature: 0.05  // 更低温度，减少随机性
```

#### 3. 题目解析结果纠错 UI（增强教学闭环）

**当前实现**：AIInput.vue 有编辑参数按钮，但不够直观

**改进方案**：
- 解析结果卡片显示所有提取的参数（可编辑输入框）
- 错误字段高亮红色边框
- 用户可直接修改 → 应用 → 重新生成场景

#### 4. 关键帧标注功能（教学价值高）

**实现方案**：
```typescript
// usePhysics.ts 的 updatePhysics 中检测关键事件
function detectKeyEvents(obj: ParticleObject): KeyEvent[] {
  const events: KeyEvent[] = []
  
  // 速度变号（最高点/最低点）
  if (prevVy * obj.vy < 0) {
    events.push({ type: 'velocity_sign_change', frame: currentFrame })
  }
  
  // 碰撞瞬间
  if (collided) {
    events.push({ type: 'collision', frame: currentFrame })
  }
  
  // 速度最大/最小
  const vMag = Math.hypot(obj.vx, obj.vy)
  if (vMag > maxVelocity) {
    events.push({ type: 'max_velocity', frame: currentFrame })
  }
  
  return events
}
```

**UI 显示**：回放进度条上标注关键帧点，点击跳转到该帧。

---

### 四、实施路线图建议

| 阶段 | 时间 | 目标 | 关键任务 |
|------|------|------|---------|
| **第 1 周** | 3 天 | 核心竞争力提升 | 预置真题库 + AI Prompt 优化 |
| **第 2 周** | 2-3 天 | 教学功能完善 | 纠错 UI + 关键帧标注 + 错误处理 |
| **第 3 周** | 3-4 天 | 稳定性保障 | 单元测试 + 错误降级 + 网络容错 |
| **第 4 周** | 视时间 | 体验优化 | 移动端适配 / 图片识别 / 3D 模式 |

---

### 五、竞赛决赛建议

**必做**（影响评审结果）：
1. ✅ 预置真题库（展示教学价值）
2. ✅ AI Prompt 优化（展示技术能力）
3. ✅ 录制演示视频（"输入题目→一键生成→运行演示"完整流程）

**加分项**（区分度）：
1. 图片识别（多模态 AI，决赛亮点）
2. 3D 模式（覆盖更多题型）
3. 单元测试（展示工程化能力）

---

需要我帮你实施哪个改进策略？推荐先做 **预置高考真题库**（最高收益，工作量适中）。