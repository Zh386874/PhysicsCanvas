# 项目规则

> 本文件是所有 AI 编码助手（Claude Code、Cursor、TRAE、Codex 等）在本项目协作时的统一规则源。`AGENTS.md` 仅作指针引用，规则以本文件为准。

## 测试纪律（不可妥协）

CRITICAL: Test integrity is non-negotiable.

测试是本项目（物理仿真引擎）的规格说明。物理定律的正确性由测试背书，篡改测试等于伪造物理正确性，会向学生传递错误的物理直觉——这是教育科技产品最严重的缺陷。

### 禁止行为

- NEVER 删除测试文件或测试用例以让测试通过
- NEVER 使用 `skip` / `xit` / `it.skip` / `describe.skip` 跳过失败测试
- NEVER 弱化或修改测试断言的期望值（如放宽 `toBeCloseTo` 精度、把 `toBe` 改成 `toBeTruthy`、把 `toBeLessThan(1e-6)` 放宽为 `toBeLessThan(1)`）
- NEVER 注释掉失败断言
- NEVER 在测试中注入运行时补丁让被测代码"临时正确"
- NEVER 为了通过测试而修改 `tests/contracts/` 目录下的任何文件

### 遇到失败测试的正确流程

1. 完整阅读测试，理解它规定的物理行为
2. 阅读实现代码，定位实现与测试规格的偏差
3. 修复【实现代码】使其符合测试规格
4. 运行测试验证
5. 不得触碰测试文件，除非获得我明确批准

### 唯一可接受的测试修改理由

- 测试本身从一开始就有 bug（期望值写错）→ 需向我说明
- 需求确实变更 → 必须先向我确认

### AI 自证流程（任务完成前必做）

完成任何涉及代码改动的任务前，AI 必须在最终回复中显式声明：

```
✅ 测试自证：
- 本次改动【修改/未修改】 tests/ 下任何文件
- 本次改动【新增/未新增】 skip 标记（it.skip / xit / describe.skip / .todo）
- 本次改动【弱化/未弱化】 任何断言（toBe→toBeTruthy、放宽 toBeCloseTo 精度等）
- 本次改动【修改/未修改】 tests/contracts/ 下任何文件
- 全量测试结果：npm run test → <通过数/总数>
```

- 若任一项为「有改动」，必须附理由并等待人工批准
- 未声明自证或声明与 `git diff` 不符视为作弊
- 失败测试修复后，自证中必须说明「修复了哪个生产代码文件/函数」而非「修改了哪个测试」

### 警告信号清单（Code Review 必查）

提交/审查时，若 diff 出现以下任一信号，必须中止并人工核查：

| 信号                  | 模式                                                         | 风险                 |
| --------------------- | ------------------------------------------------------------ | -------------------- |
| 测试文件被删除/重命名 | `git diff --diff-filter=D -- tests/`                         | 删测试让构建变绿     |
| 新增 skip 标记        | `it.skip(`/`xit(`/`describe.skip(`/`.todo(`                  | 跳过失败用例         |
| 断言数量减少          | `expect(` 计数下降                                           | 删断言绕过失败       |
| 弱断言替换强断言      | `toBe`→`toBeTruthy`、`toBeCloseTo(x, 6)`→`toBeCloseTo(x, 2)` | 放宽精度掩盖错误     |
| 期望值反向调整        | `expect(v).toBe(10)`→`expect(v).toBe(11)`                    | 改期望值适配 bug     |
| contracts 目录改动    | `tests/contracts/` 任何 M/D                                  | 篡改物理铁律         |
| 覆盖率下降 > 0.5%     | `npm run coverage:check` fail                                | 删测试导致覆盖回归   |
| 测试中注入运行时补丁  | `Object.assign`/`vi.spyOn` 临时改被测行为                    | 让被测代码"临时正确" |

### 生产/验证会话分离

为避免「同个智能体既生产代码又验证代码」导致的作弊倾向：

- **写代码会话**：AI 修改 `src/` 生产代码以让测试通过，禁止触碰 `tests/`
- **写测试会话**：AI 仅根据需求/物理定律新增测试，禁止修改 `src/` 生产代码
- 两种任务应在不同对话中完成，前一会话的「让测试变绿」压力不会污染后一会话的测试设计
- 复杂任务（如新增物理场景）应拆为两步：先写测试 → 人工确认测试合理 → 再写实现

### 覆盖率基线不可自动更新

- `scripts/.coverage-baseline.json` 是人工基线，AI **禁止**自动运行 `npm run coverage:save-baseline`
- 仅当【新增测试】导致覆盖率真实上升时，由人工执行基线更新
- 若 AI 因删测试导致覆盖率下降，`npm run coverage:check` 将 fail，此时必须恢复被删测试，**不得**通过更新基线掩盖下滑
- 基线文件本身受 `.husky/pre-commit` 检测保护（任何改动需人工 `git commit --no-verify` 并说明理由）

## 物理定律契约测试

`tests/contracts/` 目录下的测试是物理铁律的不可动摇契约（自由落体、牛顿第一定律、动量守恒、能量守恒）：

- AI 只能运行这些测试，不能修改
- AI 不得读取这些测试的期望值后反向调整生产代码以"凑通过"
- 违反此规则视为作弊
- 本地 `pre-commit` 钩子与 CI 会自动拦截对该目录的任何修改

## 通用编码规范

- 仅满足当前需求，不为"将来"预留抽象
- 同模式重复 ≥3 次再抽象
- 所有公开 API 必须含类型标注，变量名表意清晰，禁用 `data`/`info`/`temp` 等模糊名
- 避免魔法数字，用命名常量
- 改动直对请求，不做顺手重构、不改无关旧码风格
