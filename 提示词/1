你的判断很对——目前的单文件 HTML 在功能上虽然可以跑，但代码全部堆在一起，就像把所有家具塞进一个房间，以后想加个功能或改个 bug 都会很痛苦。要让项目能持续扩展成一个真正的“可搭建物理模型”的平台，选一个合适的前端框架是必要的一步。

考虑到你**没有编程基础，全程依赖 TRAE 生成代码**，框架的选择要满足三个条件：
1. **AI 容易理解和生成**（语法直观，社区成熟，代码示例多）
2. **学习曲线低**（你作为维护者也能看懂大概结构）
3. **扩展性好**（未来可以加入 3D、登录、数据库等复杂功能）

基于这三点，我推荐使用 **Vue 3 + Vite** 进行重构。

---

## 为什么选 Vue 3 + Vite？

| 对比维度 | 纯 HTML 单文件 | Vue 3 + Vite |
|----------|---------------|--------------|
| **代码组织** | 全部混在一起，上千行 | 每个功能独立成组件（如 `CanvasView.vue`、`ControlPanel.vue`） |
| **状态管理** | 全局变量满天飞，修改困难 | 响应式数据（`ref`/`reactive`），数据变界面自动变 |
| **复用性** | 复制粘贴代码 | 组件复用（比如“滑块参数组件”各场景都用） |
| **AI 生成效率** | 需描述整个文件结构 | 只需描述一个组件的功能，AI 就能生成 |
| **未来扩展** | 几乎无法接入路由、状态库等 | 可以逐步添加 Vue Router、Pinia、Three.js 等 |

Vue 3 的**单文件组件（.vue）** 将模板、脚本、样式写在一起，结构清晰，TRAE 生成起来准确率很高，而且网上 Vue 的教程和提问最多，AI 学习过的代码也最丰富。

---

## 推荐的项目结构

重构后，你的项目文件夹会是这样：

```
物理解模/
├── index.html
├── package.json
├── vite.config.js
├── src/
│   ├── main.js              // 入口文件
│   ├── App.vue              // 根组件，布局框架
│   ├── components/
│   │   ├── SceneTabs.vue    // 顶部场景切换栏
│   │   ├── ObjectList.vue   // 左侧物体列表
│   │   ├── PropertyPanel.vue // 属性编辑面板
│   │   ├── ControlBar.vue   // 播放/暂停/重置按钮
│   │   ├── PhysicsCanvas.vue // Canvas 画布 + 物理引擎
│   │   └── ForceEditor.vue  // 自定义力的编辑组件
│   ├── composables/         // 可复用的逻辑（物理引擎、碰撞检测等）
│   │   ├── usePhysics.js    // 物理模拟核心
│   │   ├── useCollision.js  // 碰撞检测
│   │   └── usePresets.js    // 预设场景加载
│   └── assets/              // 静态资源（如有）
└── public/
```

不用担心这些东西你搞不懂，**所有代码仍然由 TRAE 生成**，你只需要按提示词让它创建这些文件即可。

---

## 如何用 TRAE 一步步重构

重构不能一口吃成胖子。我为你设计了**渐进式迁移路径**，每一步都有对应的提示词，你可以直接交给 TRAE。

### 步骤 0：让 TRAE 帮你搭建 Vue + Vite 项目骨架

**提示词：**
```
请帮我创建一个 Vue 3 + Vite 项目的基础结构。项目名称“物理解模”，要求：
1. 使用 npm create vite@latest 的命令方式生成项目（你直接模拟输出所有文件内容，我会手动创建）。
2. 或者，直接给我一个最小化的 Vue 3 项目文件列表：
   - index.html
   - package.json（包含 vue 和 vite 依赖）
   - vite.config.js（基础配置）
   - src/main.js（创建并挂载 App）
   - src/App.vue（写一个简单的 Hello World）
   请告诉我每个文件的完整内容。
```

TRAE 会输出多个文件的代码。你在本地新建一个文件夹，逐个创建这些文件（文件名和代码完全对应），然后用 `npm install` 安装依赖，运行 `npm run dev` 就能看到 Vue 页面。**这个操作你不需要懂，照做就行。**

### 步骤 1：迁移布局和静态界面

**提示词：**
```
现在要把之前单文件 HTML 的静态布局迁移到 Vue 组件中。请帮我编写以下组件，整体布局要求与之前的“物理解模”一致：
1. App.vue：左侧面板（280px）+ 右侧画布 + 底部控制栏，使用 flex 布局。
2. components/SceneTabs.vue：顶部三个 Tab（抛体运动/斜面滑块/弹性碰撞），用 props 接收当前选中值，emit 切换事件。
3. components/ObjectList.vue：左侧物体列表，展示物体名称和类型，点击选中 emit 事件。
4. components/PropertyPanel.vue：根据选中的物体显示属性表单（名称、质量、位置等），v-model 双向绑定。
5. components/ControlBar.vue：播放/暂停/重置/受力显示按钮，emit 事件。
6. components/PhysicsCanvas.vue：只放一个空的 Canvas 元素，等待后续添加绘制逻辑。

确保各组件之间通过 props 和 emit 通信，不要跨组件直接操作 DOM。请输出每个 .vue 文件的完整代码。
```

### 步骤 2：实现物理引擎逻辑（可组合函数）

**提示词：**
```
请帮我创建一个 Vue 3 的可组合函数 usePhysics.js，放在 src/composables/ 目录下。它需要包含：
- 场景状态：objects 数组、forces 数组、时间、播放状态等，全部用 reactive 包裹。
- 物理更新函数 updatePhysics(dt)：遍历所有物体，计算合力（全局重力 + 物体自定义加速度），用欧拉积分更新位置速度，处理线段碰撞（调用碰撞模块）。
- 碰撞检测函数 checkCollision()：放在单独文件 useCollision.js 中，目前先实现质点与水平线段的碰撞（地面），质点间碰撞（一维弹性），返回修正后的状态。
- 重置函数 reset()：恢复到初始快照。
- 所有函数 export 出去，在 PhysicsCanvas.vue 中引入使用。
```

TRAE 会生成逻辑代码。这里就是你项目**可扩展性的核心**：未来加电磁力、弹簧力，只需要修改 `usePhysics.js` 里的力计算部分，其他地方不受影响。

### 步骤 3：连接组件与逻辑

**提示词：**
```
现在将组件与物理逻辑连接起来。修改 PhysicsCanvas.vue：
1. 引入 usePhysics，在模板中使用 Canvas 元素，mounted 后启动 requestAnimationFrame 循环。
2. 循环中根据播放状态调用 updatePhysics，然后 draw(ctx) 绘制所有物体、线段、受力箭头、轨迹。
3. 控制栏通过 props 传入播放状态，事件调用 play/pause/reset 方法。
4. 物体列表和属性面板通过 provide/inject 或直接 props 传递场景数据。
请确保数据流是单向的：场景数据在 usePhysics 中管理，组件只读取和发射用户交互事件。输出修改后的 PhysicsCanvas.vue 和 App.vue（负责组合）。
```

### 步骤 4：添加预设场景和力编辑器

**提示词：**
```
在现有 Vue 项目中：
1. 创建 usePresets.js，导出三个预设场景（平抛、斜面滑块、碰撞）的初始配置函数，返回 objects 和 forces 数组。
2. 在 SceneTabs 的切换事件中，调用对应预设函数重置场景。
3. 创建 ForceEditor.vue 组件，显示在属性面板中，可以为每个物体添加额外加速度，允许增删多组力。
4. 完善整体样式，使用蓝白科技风。
```

至此，你的项目就从一个臃肿的 HTML 文件变成了**模块化、可多人协作、可轻松扩展**的现代前端项目。

---

## 关于初赛与复赛的衔接

- **初赛提交**：继续用当前单文件 HTML 的最终版打包成 Zip 提交，完全合规，能进复赛就行。
- **重构项目**：上面的 Vue 重构过程正好是你**复赛阶段**的打磨内容。复赛作品要求更完善的项目结构和代码质量，你用 Vue 重构后，可以配上更丰富的场景、AI 题目导入接口等，竞争力会大增。
- **TRAE 支持**：Vue 单文件组件生成，TRAE 操作很熟练，你只需给出清晰的组件描述，它就能给出可靠的代码。

你完全可以按这套路径前进：**先用单文件拿到复赛门票，同步用 Vue 重构准备复赛作品**。需要我调整任何部分的提示词，或者想直接开始某一步，随时告诉我。
