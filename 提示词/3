你的项目分析报告非常专业。我在 IDE 里打开工程文件，结合你的发现给出可落地的改进方案。

---

## 当前问题优先级修复表

| 优先级 | 问题 | 影响 | 修复方式 |
|--------|------|------|----------|
| **P0** | 物理单位不统一 | 平抛/碰撞场景“几乎不动”，评审体验差 | 统一引入缩放因子 |
| **P0** | AIInput 直接 import state | 数据流不一致，评审扣分 | 改为 emit 事件 |
| **P1** | 快照不含 field/groundY | 回放时场设置丢失（当前刚好没 bug） | 扩展快照结构 |
| **P1** | 线段法线需手动重算 | 用户改端点后易出错 | 端点变化自动重算 |

---

## P0-1：统一物理尺度

**问题根源**：`usePhysics.js` 里重力用的是 `state.gravity`，但不同预设给的值不一样——斜面用 300（像素尺度），平抛/碰撞用 9.8（SI）。需要统一为像素尺度，所有预设按同一比例尺换算。

**修复提示词（交给 TRAE）：**

```
在我当前的 Vue 3 物理解模项目中，需要修复物理参数尺度不统一的问题。

### 问题描述
- usePhysics.js 中重力加速度 state.gravity 在不同预设中被赋予不同值：斜面用 300，平抛用 9.8
- 原因：斜面预设把重力按“像素/s²”直接赋值为 300（对应约 30 m/s² 放大 10 倍），而平抛预设误用了 SI 单位的 9.8
- 导致平抛和弹性碰撞场景中物体几乎不动

### 修复要求
1. 在 usePhysics.js 顶部定义全局缩放常量：`const PIXELS_PER_METER = 50`（1米 = 50像素）
2. 重力统一使用像素尺度：state.gravity 默认值 = 9.8 * 50 = 490（像素/s²）
3. 修改 usePresets.js 中所有预设：
   - 平抛：vx 从 200 改为 10*50=500，vy 保持 0，重力在场景中统一设
   - 弹性碰撞：v1 从 100 改为 8*50=400，v2 从 -50 改为 -3*50=-150
   - 斜面滑块：重力相关已有，保持斜面预设其他参数不变
   - 磁场圆周：vx 保持 150，但需确认半径是否合理
   - 电场偏转：vx 从 150 改为 300
4. 在 PropertyPanel.vue 或场景全局设置区，将重力显示值改为可读形式：
   - 显示为“重力加速度: 9.8 m/s²”（固定转换显示，实际内部用像素值）
   - 用户修改时按 m/s² 输入，内部自动 ×50

### 约束
- 不要引入新的 npm 包
- 确保所有五个预设场景的动画速度恢复正常
- 保持斜面场景的碰撞行为不变

输出所有需要修改的文件的完整代码。先输出 usePhysics.js，再输出 usePresets.js，最后输出涉及的其他文件。
```

**验证标准**：平抛预设的小球应在约 1-2 秒内落到地面，看起来像真实的平抛运动。

---

## P0-2：AIInput 数据流规范化

**问题根源**：`AIInput.vue` 里直接 `import { state } from '../composables/usePhysics.js'`，然后调用 `loadScene`。这破坏了 Vue 单向数据流原则——组件不应该直接修改全局状态，应该通过 emit 让父组件 App.vue 来操作。

**修复提示词：**

```
在我的物理解模 Vue 项目中，需要修复 AIInput.vue 的数据流问题。

### 问题描述
当前 AIInput.vue 直接 import usePhysics 的 state，并在组件内部调用 loadScene 修改全局场景状态。这违反了 Vue 单向数据流原则（组件应通过 emit 通知父组件，由父组件操作状态）。

### 修复要求
1. 修改 AIInput.vue：
   - 删除 `import { state, loadScene } from '../composables/usePhysics.js'`
   - 新增 emit 定义：`const emit = defineEmits(['load-preset', 'update-params'])`
   - 在 parseAndLoad 函数中，不直接调用 loadScene，改为：
     - emit('load-preset', presetName)  // 通知父组件加载哪个预设
     - emit('update-params', { key: value, ... })  // 传递解析出的参数
   - 移除组件内对 usePhysics 的所有直接引用

2. 修改 App.vue：
   - 在模板中给 AIInput 绑定事件：
     `<AIInput @load-preset="handleLoadPreset" @update-params="handleUpdateParams" />`
   - 实现 handleLoadPreset(presetName)：调用 usePhysics 的 loadScene
   - 实现 handleUpdateParams(params)：遍历 params 更新对应物体属性
   - 可以给解析过程加一个过渡动画（画布短暂显示“AI 解析中...”）

3. 修改 usePhysics.js（如果需要）：
   - 导出一个 `updateObjectProperty(id, key, value)` 函数，方便 App.vue 调用
   - 该函数找到对应物体并修改属性

### 约束
- AI 解析的用户体验保持不变（输入题目 → 按钮加载 → 1.5秒后显示结果 → 自动加载场景）
- 所有现有功能不受影响
- 输出需要修改的文件的完整代码

先输出 AIInput.vue 的修改后代码，再输出 App.vue 的修改后代码，最后输出 usePhysics.js 中新增的导出函数。
```

**验证标准**：输入"一个物体从斜面上滑下" → 点击解析 → 斜面滑块场景正常加载。在 Vue DevTools 中检查数据流是父组件到子组件单向的。

---

## P1-1：快照存储完整状态

**修复提示词：**

```
在我的物理解模 Vue 项目 usePhysics.js 中，需要修复快照不完整的问题。

### 问题描述
当前 pushSnapshot 函数只存储了物体的 {id, x, y, vx, vy}，但没有存储 field（电场/磁场设置）、groundY（地面位置）等全局状态。如果在回放过程中这些全局状态发生变化，回放画面将不正确。

### 修复要求
1. 修改 pushSnapshot 函数，将快照结构扩展为：
   {
     objects: [{id, x, y, vx, vy, ...}],  // 保持现有结构
     field: { type, E, B },                // 当前场设置
     groundY: state.groundY,               // 地面 y 坐标
     gravity: state.gravity,               // 重力加速度
     timestamp: Date.now()                 // 可选，用于调试
   }

2. 修改 Timeline.vue 中的恢复逻辑：
   - 当用户拖动时间轴到某一帧时，除了恢复物体位置速度
   - 同时恢复 field 设置（如果当前 field 与快照不同，更新 state.field）
   - 同时恢复 groundY（如果变化）
   - 这些恢复应该触发画布重绘

3. 修改 PhysicsCanvas.vue 中的绘制逻辑：
   - 回放模式下，使用快照中的 field 和 groundY 来绘制场可视化和地面
   - 而不是使用当前 state 中的值

### 约束
- 快照大小会略增，但仍在可接受范围（每帧多存储约 100 字节）
- 保持向后兼容：如果旧快照没有 field 字段，回退使用当前 state 的值
- 输出需要修改的文件的完整代码
```

---

## P1-2：线段法线自动重算

**修复提示词：**

```
在我的物理解模 Vue 项目中，需要优化线段法线设置的用户体验。

### 问题描述
当前用户在 PropertyPanel 中修改线段端点坐标后，法线不会自动更新，需要手动点击法线方向按钮。这容易导致碰撞检测行为与预期不符。

### 修复要求
1. 在 usePhysics.js 或 useCollision.js 中新增函数：
   ```
   autoComputeNormal(segment)
   ```
   - 根据线段方向自动计算默认法线（垂直于线段，默认指向“上方”或“左侧”）
   - 计算逻辑：取线段方向向量 (dx, dy)，法线 = (-dy, dx) 归一化（即线段方向的逆时针旋转 90°）
   - 如果用户之前手动设置过法线方向（如“向下”），保留用户选择的方向偏好

2. 在 PropertyPanel.vue 中：
   - 当用户修改线段端点坐标（x1/y1/x2/y2 任一输入框变化）时，自动调用 autoComputeNormal
   - 法线方向预设按钮仍然保留，方便用户主动切换方向（如斜面需要法线指向“上方”还是“下方”）
   - 在法线预设按钮旁显示当前法线向量值（如“法线: (-0.707, -0.707)”），方便确认

3. 添加法线方向的可视化预览：
   - 在画布上绘制线段时，从线段中点沿法线方向画一个短箭头（长度 20px）
   - 箭头颜色用浅紫色，半透明，帮助用户理解碰撞面的“内侧”在哪

### 约束
- 自动计算的法线方向应与现有预设的行为一致（斜面法线指向斜面“上方”即滑块所在侧）
- 不要引入新的依赖
- 输出需要修改的文件的完整代码
```

---

## 建议修复顺序

| 顺序 | 修复项 | 预期轮数 | 理由 |
|------|--------|----------|------|
| **1** | 物理尺度统一 | 2-3 轮 | 修复后所有场景动画速度正常，截图效果好 |
| **2** | AIInput 数据流 | 1-2 轮 | 规范性修复，评审能看出代码质量 |
| **3** | 快照完整性 | 1-2 轮 | 功能性修复，时间轴回放更准确 |
| **4** | 法线自动计算 | 1-2 轮 | 体验优化，减少用户出错 |

每完成一步都截图 + 保留 Session ID。四个修复加上之前四个核心功能的开发，你的实践过程会比要求更丰富。

需要我开始输出第一项（物理尺度统一）的修复吗？