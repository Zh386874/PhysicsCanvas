分步实施提示词（直接复制给 Trae）
第 1 步：重构弧线工具（任意角度 + 减少线段 + 弧线显示）
text
在 PhysicsCanvas.vue 中，重构弧线绘制逻辑：
1. 修改弧线工具的交互流程为三次点击：
   - 第一次点击：确定圆心 (cx, cy)，状态标记为 'arc_set_center'
   - 第二次点击：确定半径终点 (rx, ry)，同时作为弧线的起点角度，计算半径 = distance(cx,cy, rx,ry)，起始角度 = atan2(ry-cy, rx-cx)
   - 第三次点击：确定弧线终点角度，通过鼠标相对于圆心的角度计算，实时预览弧线，点击确认最终角度。
   简化版（推荐）：第二次点击既确定半径又作为起点，第三次点击只需在圆周上任意点决定终点角度，弧线从起点逆时针延伸到终点角度（也可按住Shift反向）。
   或者更简单：第一次点击圆心，第二次点击半径+起点，第三次点击终点角度（鼠标移动显示预览扇区，点击确认）。实现为：
   - arcState = { centerX, centerY, radius: null, startAngle: 0, endAngle: 0, phase: 'center' }
   - 在 onMouseDown：如果 phase=='center'，记录圆心，phase='radius'
     else if phase=='radius'，计算半径和起始角度，phase='angle'
     else if phase=='angle'，计算终点角度，生成弧线段并添加，重置 phase='center'。
   - 在 onMouseMove 中，根据当前鼠标位置实时更新预览：半径阶段显示圆和半径线；角度阶段显示从起点到当前角度的弧线。
2. 生成弧线物体时，不再创建大量独立线段对象，而是创建一个**特殊的“弧线”对象**：
   { id: uniqueId, type: 'arc', cx, cy, radius, startAngle, endAngle, friction: 0.5 }
   同时在物理模拟中自动将其离散为 8 条线段用于碰撞检测（在 useCollision.js 中新增对 'arc' 类型的处理，内部离散为临时线段数组，不存入全局 objects）。
   但为了最小改动，可以继续在添加时生成 8 条线段并添加到 objects，但用统一的 groupId 标记它们属于同一弧线，以便后续框选或删除时视为整体。
   提示词建议：**直接生成 8 条线段添加到 objects**，但设置它们的 groupId 为弧线 ID，并在 ObjectList 中可按组显示。渲染时绘制一条平滑弧线覆盖在线段上（仅为视觉效果）。
   - 生成线段函数 generateArcSegments(cx, cy, radius, startAngle, endAngle, numSegments=8) 返回线段数组。
3. 渲染弧线预览时，使用 ctx.arc 绘制真正的弧线（半透明），并在生成后，在 draw 函数中检测 type=='arc' 时绘制弧线，否则绘制线段。
   - 由于线段已经是 objects 中的独立项，弧线的视觉可以放在所有线段绘制之后，额外画一条弧线（基于 groupId 去重，取最小角度和最大角度，重新绘制一次）。
   - 简单做法：遍历 objects，对于所有 groupId 相同且 type='line_segment' 的组，计算整体 arc 参数，用 ctx.arc 绘制平滑弧线（颜色稍亮），然后跳过绘制这些线段？或者保留线段绘制但额外加弧线描边。建议保留碰撞用的线段绘制（细线），再叠加粗弧线描边。
4. 更新工具栏：将圆弧按钮保留，提示文字改为“三次点击绘制弧线（圆心→半径起点→终点）”。
5. 验证：选择圆弧工具，点击圆心位置，移动鼠标看到圆预览，点击确定半径，再移动鼠标看到弧线预览，点击确定。生成 8 条线段组成的弧线，画布上可见平滑弧线描边。小球可与弧线碰撞。
检验方法：

在自定义场景，点击圆弧工具，在画布上点三次（圆心、半径点、终点角度），生成一条弧线。

画布上应看到弧线看起来是平滑的，而不是明显的折线。

添加小球放到弧线上方，播放，小球沿弧面运动。

第 2 步：实现场景自动保存/恢复（防丢失）
text
在 usePhysics.js 或 App.vue 中增加 localStorage 自动保存逻辑：
1. 每当自定义场景的 objects 发生变化时（添加、删除、移动物体），自动将 state.objects 序列化存入 localStorage，key 为 'custom_scene_objects'。
   可以使用 vue 的 watch，在 App.vue 中监听 state.objects 的深度变化（谨慎性能，可用防抖）。
   或者，在每次编辑操作（addObject/removeObject/updateObjectProperty）后手动触发保存。
2. 在自定义场景的 loadScene 时，先检查 localStorage 中是否有保存的 objects：
   - 如果有，弹窗询问“是否恢复上次未保存的场景？” 确认后加载，否则清空。
   - 或者更无缝：直接恢复，并在工具栏显示“已恢复上次场景”。重置按钮也不再清空，而是恢复到最近一次播放前的快照。
3. 调整重置逻辑：在自定义场景下，点击“重置”应该恢复到上一次播放前保存的快照，而不是初始空白。实现方法：在点击“播放”时，保存当前 objects 的深拷贝到 customSnapshot；重置时用这个快照恢复。
   - 同时，退出播放暂停后，objects 保持当前运动后的状态，但用户可再编辑。若此时点重置，应用 customSnapshot 恢复。
   - customSnapshot 在每次播放开始时更新。
4. 额外增加一个“新建场景”按钮（可选），用于主动清空并保存空白到 localStorage。
5. 验证：搭建一个场景（小球+平台），点击播放，小球运动，暂停后点击重置，场景恢复到播放前的初始位置（而不是消失）。关闭浏览器标签页重新打开，自定义场景自动恢复上次的 objects（若实现了 localStorage 恢复）。
检验方法：

搭建场景 → 播放 → 暂停 → 点重置，场景恢复到刚刚搭建好的状态。

关闭页面，重新打开，点击自定义标签，看到之前搭建的场景仍存在。

第 3 步：右键框选与批量操作
text
在 PhysicsCanvas.vue 中实现矩形选框功能：
1. 监听 mousedown 事件，当 event.button === 2（右键）时，阻止默认菜单，进入框选模式：
   - 记录 selectionStart = 世界坐标
   - selectionActive = true
   - 暂时不执行拖拽或添加逻辑。
2. 监听 mousemove，如果 selectionActive，计算当前鼠标的世界坐标，绘制选择矩形（半透明蓝色边框，填充极淡蓝色）。
   - 实时计算出矩形范围内的物体，并临时高亮它们（用不同颜色描边）。
3. 监听 mouseup，如果 selectionActive，确定最终矩形，找出所有位于矩形内的物体：
   - 对于圆：圆心在矩形内
   - 对于线段：两个端点至少一个在矩形内（或整个线段与矩形相交，简化：端点任一在内即可）
   - 将这些物体的 id 存入 selectedIds 数组（多选）。
   - 在 ObjectList 中同步高亮这些物体（需要传递 selectedIds 给 ObjectList，或者在 App 层管理多选状态）。
   - 释放 selectionActive。
4. 在 App.vue 中增加响应式变量 selectedIds（数组），传递给 PhysicsCanvas 和 ObjectList。
5. 批量拖拽：当 selectedIds 包含多个物体时，拖拽其中任意一个物体，整个选择组整体平移。
   - 在 onMouseDown 的拖拽逻辑中，如果点击的物体 id 在 selectedIds 内，且 selectedIds.length > 1，则设置 dragTarget 为整个组（记录所有物体初始位置，计算相对偏移），在 onMouseMove 中同时平移组内所有物体。
6. 批量删除：按 Delete 键时，如果 selectedIds.length > 0，删除 selectedIds 中的所有物体（调用 removeObject），然后清空 selectedIds。
7. 右键点击空白区域取消选中：如果右键点击时没有任何物体被框选，则清空 selectedIds。
8. 在 draw 函数中，为 selectedIds 中的物体绘制高亮边框（蓝色发光）。
9. 验证：在画布上右键按住拖拽出一个矩形，松开后矩形内的物体边框变蓝；再按住其中一个物体拖拽，全部选中物体一起移动；按 Delete 全部删除。
检验方法：

添加三个小球，右键框选其中两个 → 两个小球高亮。

拖拽其中一个小球，两个一起动。

按 Delete，两个小球被删除，剩下一个未选中的小球。

第 4 步：Shift 防重叠放置
text
在 PhysicsCanvas.vue 的 onCanvasClick（添加小球逻辑）中增加重叠检测：
1. 在创建小球对象前，检查新球是否与现有场景物体重叠：
   - 对于圆：判断圆心距 < r1 + r2 + 阈值（如 2px）
   - 对于线段：计算圆心到线段的距离 < radius + 阈值
2. 如果没有重叠，直接添加。
3. 如果有重叠且 event.shiftKey 为 true，则计算分离方向：
   - 找到重叠最严重的物体或线段，计算法线方向（从障碍物指向新球中心），沿该方向移动新球位置，直到距离 ≥ radius + 线段所需距离（若为线段则考虑半径+阈值）。
   - 可以多次迭代小步移动（每次 1px）直到不重叠，限制最大尝试次数避免死循环。
4. 移动后检查是否移出画布，如果是则放弃（或提示无法放置）。
5. 无论是否按下 Shift，如果重叠但不按 Shift，则仍然在原位置添加（用户可能需要调整），但可以在控制台警告或短暂闪烁提示。
6. 添加后，如果按住 Shift 且发生了移动，给一个微小动画（如短暂闪绿）表示已修正。
7. 验证：在已有线段上按住 Shift 点击，小球自动跳到线段上方表面，不重叠。不按 Shift 点击，小球与线段重叠（穿透），但后续可拖拽。
检验方法：

画一条斜线段，按住 Shift 在线段上点击，小球出现在线段表面上方（不嵌入）。

松开 Shift 再点击，小球与线段重叠（部分嵌入），但依然存在。