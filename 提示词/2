你这份差距分析非常到位，直击要害。初赛 Demo 不需要面面俱到，但必须**证明创意核心逻辑走得通**。按你的优先级，我给出可以直接交给 TRAE 的分段提示词，在 Vue 项目基础上逐个补上关键缺口。

---

## 补缺路线图

| 优先级 | 功能 | 实施方案 | 预计新增代码 |
|--------|------|----------|-------------|
| **P0** | 分步回放 | 时间轴组件 + 关键帧快照存储 | 新组件 `Timeline.vue`，修改 `usePhysics.js` |
| **P1** | 电磁场场景 | 新增预设 + 洛伦兹力计算 | 修改 `usePhysics.js` 力的计算，新增预设 |
| **P1** | 真实斜面 | 线段碰撞检测 + 斜面预设修正 | 修改 `useCollision.js`，重做斜面预设 |
| **P2** | AI 解析入口 | 输入框 + 模拟解析结果展示 | 新组件 `AIInput.vue`，后端接口留空 |

---

## P0：分步回放功能

**核心思路**：在物理模拟的每一帧，把场景状态（所有物体的位置、速度）存入一个快照数组。用户拖动时间轴滑块时，直接跳到对应帧的状态渲染，而不是重新计算。

**提示词（给 TRAE）：**

```
在我现有的 Vue 3 物理解模项目中，需要新增“分步回放”功能。请帮我完成以下修改：

### 1. 在 usePhysics.js 中添加快照存储
- 新增一个 `snapshots` 数组（用 ref([])），最大存储 600 帧（10秒 × 60fps）。
- 在动画循环的每一帧，物理计算完成后，将当前所有物体的 { id, x, y, vx, vy } 深拷贝存入 snapshots。
- 播放状态下正常推入；暂停时不推入；重置时清空 snapshots。
- 导出一个 `snapshots` 和当前帧索引 `currentFrame`。

### 2. 新建 components/Timeline.vue
- Props：snapshots（Array）、currentFrame（Number）
- Emit：update:currentFrame（拖动时发射）
- 模板结构：
  - 一个水平滑块 `<input type="range">`，最小值 0，最大值 snapshots.length - 1，绑定 currentFrame
  - 滑块下方显示当前帧号 / 总帧数（如 “第 45 帧 / 共 200 帧”）
  - 左右两侧各一个按钮：◀ 上一关键帧、▶ 下一关键帧
- 关键帧定义：物体速度方向发生明显变化（速度分量符号改变）的帧，或是碰撞发生的帧。暂时简单实现：在 snapshots 中标记速度 vx 或 vy 符号变化的帧为关键帧，存储关键帧索引数组 `keyframeIndices`，点击上下关键帧按钮时跳到最近的关键帧索引。
- 样式：滑块充满宽度，高度 20px，带刻度感。

### 3. 修改 PhysicsCanvas.vue
- 新增模式 prop：mode（'live' | 'replay'），默认 'live'
- 当 mode === 'replay' 时：
  - 暂停物理更新循环
  - 根据 currentFrame 从 snapshots 中取出对应帧的状态，绘制到 Canvas
  - 绘制时在画布右上角显示 “回放模式” 水印
- 当用户拖动时间轴时，emit 'seek' 事件，父组件更新 currentFrame

### 4. 修改 ControlBar.vue
- 新增按钮 “⏪ 分步回放”，点击后在播放和回放模式之间切换
- 回放模式下，“播放/暂停”按钮变为灰色不可用

### 5. 修改 App.vue
- 引入 Timeline 组件，放在画布下方
- 管理 mode 状态（'live' / 'replay'），传给 PhysicsCanvas 和 ControlBar
- 回放模式下显示 Timeline

确保所有现有功能不受影响，输出所有需要修改的文件的完整代码。
```

**保存 & 测试**：确认时间轴拖拽时画面能回溯到之前的状态，关键帧跳转正常。记录此步 Session ID。

---

## P1-1：电磁场场景

**核心思路**：在 `usePhysics.js` 中增加洛伦兹力计算（F = qv × B）。带电粒子在磁场中做匀速圆周运动，这是高考超高频考点。

**提示词：**

```
在我的物理解模 Vue 项目中，新增电磁场场景支持。请完成以下修改：

### 1. 修改 usePhysics.js —— 新增场力系统
- 在场景状态中新增 `field` 对象：
  {
    type: 'none' | 'electric' | 'magnetic',
    // 电场
    E: { x: 0, y: 0 },     // 电场强度矢量 (N/C，简化单位)
    // 磁场
    B: 0,                   // 磁感应强度 (T，方向垂直纸面向里为正)
  }
- 修改力的计算函数 `computeForces(object)`：
  - 保留原有重力 + 自定义力
  - 如果 field.type === 'electric' 且物体有 charge 属性：
    Fe = { x: charge * E.x, y: charge * E.y }
  - 如果 field.type === 'magnetic' 且物体有 charge 属性：
    Fm_x = charge * vy * B   （洛伦兹力 x 分量）
    Fm_y = -charge * vx * B  （洛伦兹力 y 分量，垂直纸面向里的磁场，正电荷受力 = qv × B）
    注意：vx, vy 是物体当前速度
  - 将场力加入总加速度计算
- 在物体属性中新增可选字段 `charge`（默认 0，表示不带电）

### 2. 修改 PropertyPanel.vue
- 当选中的物体是质点时，属性面板新增一行：“电荷 (q)”输入框，默认 0，单位用相对值（正负均可）
- 在场景全局属性区（可放在面板底部或新增一个折叠区），增加“场设置”：
  - 下拉选择：无场 / 匀强电场 / 匀强磁场
  - 选择电场时：显示 Ex、Ey 输入框
  - 选择磁场时：显示 B 输入框 + 方向说明（正值 = 垂直纸面向里 ⊙，负值 = 向外 ⊗）
- 修改时通过 emit 更新场景 field 对象

### 3. 修改 usePresets.js —— 新增电磁预设
- 新增预设“带电粒子在匀强磁场中的圆周运动”：
  - 物体：质点，位置(350, 250)，质量 1，vx=150, vy=0，charge=1
  - 场景 field：{ type: 'magnetic', B: 1 }
  - 不添加重力（或在 UI 中提供关闭重力的选项）
  - 预期效果：粒子做逆时针匀速圆周运动
- 新增预设“带电粒子在匀强电场中的抛物线运动”：
  - 物体：质点，位置(50, 400)，质量 1，vx=150, vy=0，charge=1
  - 场景 field：{ type: 'electric', E: { x: 0, y: -50 } }（电场向下）
  - 有重力
  - 预期效果：类似平抛但加速度不同

### 4. 修改 PhysicsCanvas.vue —— 场可视化
- 当 field.type === 'magnetic' 时，在画布背景上绘制均匀分布的“⊗”或“⊙”符号（根据 B 的正负），表示磁场方向
- 当 field.type === 'electric' 时，在画布背景上绘制均匀箭头表示电场方向
- 符号半透明，不遮挡物体

输出所有修改文件的完整代码。确保力学场景不受影响（field.type === 'none' 时行为不变）。
```

**测试重点**：加载磁场预设后，粒子应画圆；调大 B 值圆半径变小，调小 B 值圆半径变大。记录 Session ID。

---

## P1-2：真实斜面（线段碰撞检测）

**核心思路**：你的 `useCollision.js` 目前可能只做了地面碰撞（y=0 水平线）。需要加入一般线段（任意角度）与质点的碰撞检测与响应。

**提示词：**

```
在我的物理解模 Vue 项目中，需要实现真实的斜面碰撞检测。目前碰撞检测只处理了水平地面。请帮我升级 useCollision.js。

### 1. 线段与质点的碰撞检测函数 detectSegmentCollision(object, segment)
segment 对象结构：{ x1, y1, x2, y2, normalX, normalY }（法线已由用户设定，指向“内侧”）

检测逻辑（逐帧）：
- 计算质点上一步位置 (prevX, prevY) 到当前位置 (x, y) 的线段
- 判断质点移动路径是否穿过了线段（用叉积判断两点是否在线段两侧）
- 如果穿过：
  - 计算路径与线段的精确交点 (hitX, hitY)
  - 将质点位置修正到交点处（沿法线方向略微偏移 1px 防止再次穿透）
  - 计算质点速度在法线方向的分量 v_normal = vx * nx + vy * ny
  - 如果 v_normal < 0（即质点正在向线段内侧移动/穿透方向）：
    - 反射法向速度：vx -= (1 + restitution) * v_normal * nx
    - vy -= (1 + restitution) * v_normal * ny
    - restitution（恢复系数）默认 0.3，作为线段属性
  - 返回 true 表示发生了碰撞

### 2. 修改 usePhysics.js 的更新循环
- 在物体位置更新后，遍历场景中所有 type === 'line_segment' 的物体
- 对每个质点调用 detectSegmentCollision
- 如果碰撞发生，标记该帧为“碰撞帧”（用于分步回放的关键帧识别）

### 3. 新增线段物体的法线设置
- 在 PropertyPanel.vue 中，当选中的是线段时，显示“法线方向”设置：
  - 预设按钮：↑上、↓下、←左、→右、↖左上（45°）、↗右上（-45°）
  - 点击后自动计算 normalX, normalY 并更新线段属性
- 在画布上绘制线段时，在法线方向一侧绘制半透明面（浅灰色填充），标明“内侧”（实体面）

### 4. 修改斜面预设
- 预设“斜面滑块”改为：
  - 斜面线段：端点(150, 420)到(450, 160)，法线方向指向左上（垂直于斜面，nx≈-0.707, ny≈-0.707，即法线指向斜面“上方”）
  - 滑块质点初始位置在斜面顶端(420, 180)，质量3，速度0
  - 地面线段：y=50（对应画布底部），法线向上
  - 仅重力作用
  - 滑块沿斜面下滑，到达底部后停止

### 5. 斜面受力分析
- 滑块在斜面上时，受力分解为：
  - 红色重力（竖直向下）
  - 紫色支持力（垂直于斜面向上）
  - 橙色摩擦力（沿斜面向上，如果摩擦系数>0）
- 支持力大小 = mg*cos(α)，摩擦力 = μ*N，用注释标注在受力箭头旁（可选）

输出所有修改文件的完整代码。确保水平地面碰撞和电磁场场景不受影响。
```

**测试重点**：加载斜面预设后，滑块沿斜面加速下滑，滑到底部后停止在水平地面上。调节斜面角度（修改线段端点）后行为正确。记录 Session ID。

---

## P2：AI 题目解析入口（初赛做“壳”）

**核心思路**：初赛阶段不接真实 AI API，但做一个**可演示的界面流程**，证明产品逻辑成立。输入框接收题目文字，模拟 AI 解析，直接加载对应的预设场景。

**提示词：**

```
在我的物理解模 Vue 项目中，新增 AI 题目解析入口组件（初赛版，先做前端壳）。

### 1. 新建 components/AIInput.vue
- 模板结构：
  - 顶部一个醒目的输入区域，包含：
    - 大号文本输入框（textarea，4行），placeholder：“请输入物理题目描述，例如：一个质量为2kg的滑块从倾角30°的光滑斜面顶端由静止释放...”
    - 按钮“🔍 AI 解析并生成模拟”（蓝色，突出）
  - 点击按钮后：
    - 按钮变为加载状态（转圈动画）
    - 1.5秒后（模拟 AI 思考时间），显示“解析结果”卡片
    - 卡片内容：提取的物理量（物体质量、初始速度、斜面角度等）+ “✅ 已生成模拟场景，请查看画布”
    - 同时自动将对应预设加载到场景中
- 解析逻辑（Mock）：
  - 用简单的关键词匹配来模拟 AI 解析：
    - 如果题目包含“平抛”/“水平抛出” → 加载平抛预设
    - 如果包含“斜面”/“倾角”/“滑下” → 加载斜面滑块预设
    - 如果包含“磁场”/“洛伦兹”/“圆周” → 加载磁场圆周预设
    - 如果包含“碰撞”/“相撞” → 加载碰撞预设
    - 其他 → 显示“正在理解中，请尝试经典场景”
  - 如果能从题目中提取数字（如“30°”“2kg”“10m/s”），自动填入对应参数
- 显示提示文字：“🤖 当前为演示版，接入真实 AI 后可解析任意题目”

### 2. 修改 App.vue 布局
- 将 AIInput 放在左侧面板顶部（在物体列表上方），占用约 200px 高度
- 如果用户觉得占空间，可以提供折叠按钮

### 3. 交互优化
- 解析完成后，画布自动切换到对应场景，并自动开始播放
- 在画布左上角短暂显示提示：“AI 已解析：斜面滑块场景”（3秒后消失）

输出所有需要新建或修改的文件的完整代码。
```

---

## 当前建议的操作顺序

| 顺序 | 任务 | 预计 TRAE 交互轮数 | 价值 |
|------|------|-------------------|------|
| **1** | 分步回放（P0） | 2-3轮 | 直接兑现报名帖承诺，评审一眼看到核心功能 |
| **2** | 电磁场场景（P1） | 2-3轮 | 证明场景可扩展，覆盖“电磁场”卖点 |
| **3** | 真实斜面（P1） | 3-4轮 | 证明物理引擎不是玩具，有真实碰撞检测 |
| **4** | AI 解析入口（P2） | 1-2轮 | 展示产品完整逻辑闭环，复赛可接真实 API |

每完成一步，都在浏览器中跑通并截图。最终提交时，你会有至少 4 个 Session ID + 4 张关键功能截图，完全满足初赛帖“实践过程”的要求。