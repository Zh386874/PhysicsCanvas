当前功能落地情况
已完成 AI 读题直接生成物理模型 的核心链路的实现：

多场支持（电场+磁场+重力可同时作用）

增强版 AI Prompt（含 few-shot 示例，输出结构化 JSON）

场景生成器（解析 AI 结果，自动构建物体、设置场、自适应画布缩放）

前端集成（输入高考题 → AI 解析 → 自动构建并显示）

下一步完善计划
🔴 高优先级（建议近期完成）
修复重力随自动缩放而失调的 bug

当前 useSceneBuilder 会根据物体范围重新计算 effectivePpm，但未同步调整 state.gravity。

正确做法：state.gravity = 9.8 * effectivePpm（默认 9.8 m/s²）。

否则缩放后物体下落加速度看起来会不正常。

增加参数微调 UI

AI 解析结果展示卡中，增加“编辑参数”按钮，允许用户修改质量、初速度、场强等。

点击“应用修改”后重新调用 buildScene，实现“AI生成+人工修正”闭环。

补充更多 few-shot 示例

当前 Prompt 只含 3 个典型场景，后续可增加：

弹簧振子（简谐运动）

传送带问题

板块模型（滑块在木板上滑动）

每增加一个，AI 对同类题型的解析准确率会明显提升。

🟡 中优先级（可逐步实现）
多过程模拟支持

高考题经常分阶段（先匀加速、后碰撞、再圆周运动）。

简易方案：在 AI 输出中增加 stages 数组，每个阶段包含不同的物体约束或受力，前端按时间顺序切换。

复杂方案：让物理引擎自然演化（依赖初态设置），多数题可覆盖。

错误处理与降级

AI 返回非法 JSON 或缺失关键字段时，给出友好提示，并回退到旧版关键词匹配 Mock 模式。

增加 API 超时、网络异常等容错。

真题库联动

预置 20 道高考真题，每道题绑定一个 JSON 场景，点击即可加载运行。

可作为“AI 无法解析”时的备选方案，也方便教学演示。

🟢 低优先级（视时间精力决定）
图片识别（多模态）

集成 GPT-4V 或国产视觉模型，上传题目截图直接生成场景。

成本较高，可作为决赛亮点。

移动端只读演示

为手机浏览器提供预设场景播放 + AI 生成场景的只读回放，方便课堂互动。

自动化测试

用 Vitest 编写物理正确性测试（碰撞后速度、落地时间等），保障模型可信度。

文档与演示视频

录制“AI 一步生成物理模型”的操作流程，用于竞赛展示。

当前可测试的题目示例
以下题目可直接在 AI 输入框中粘贴测试（需配置 API Key）：

题目1：质量 2 kg 的滑块从倾角 30° 的光滑斜面顶端由静止释放，斜面高 5 m。
题目2：带正电粒子质量 1e-10 kg、电荷 1e-5 C 以 100 m/s 水平进入 B=0.5 T 垂直纸面向里的磁场。
题目3：在 E=1000 N/C 方向向下的电场和 B=0.2 T 磁场中，带电粒子以 400 m/s 水平向右运动。

若解析成功，画布上将自动生成包含对应物体的可运行物理模型。